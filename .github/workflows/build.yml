name: Greeting on variable day

on:
  workflow_dispatch

jobs:
  greeting_job:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - run: echo "$TEST"
        env:
          TEST: ${{ vars.TEST }}
